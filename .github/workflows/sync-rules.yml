name: Update DNS Blocklist
on:
  schedule:
    - cron: '0 0 * * *'  # 每天 00:00 UTC (北京时间 08:00)
  workflow_dispatch:
jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 120  # 2小时超时（安全裕度）
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install Dependencies
        run: pip install dnspython requests
      - name: Run Update Script
        run: python update_dns_blocklist.py
      - name: Commit Changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add final_rules.txt
          git commit -m "Update DNS blocklist (auto)" || echo "No changes"
          git push
